{
  "tabs": [
    {
      "id": "general",
      "label": "General",
      "fields": [
        { "key": "debug_mode", "label": "Debug logging", "component": "checkbox" },
        { "key": "token_manager_email", "label": "Token manager (email) — tokens paid via balance are transferred to this user; leave empty to debit only", "component": "input", "inputType": "text" }
      ]
    },
    {
      "id": "rates",
      "label": "Token rates",
      "fields": [
        { "key": "rates", "label": "Currency → price per token (JSON object, e.g. {\"USD\": 0.05})", "component": "input", "inputType": "text" }
      ]
    }
  ]
}
